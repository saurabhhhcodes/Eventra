This directory contains static front-end assets used by the app.

NOTE: There is a misspelled 'src/assests/' directory that contains legacy files.
Consider consolidating or removing it after confirming no runtime references.
